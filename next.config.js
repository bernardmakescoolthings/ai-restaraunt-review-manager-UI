/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable if you need to handle images from external domains
  images: {
    domains: ['localhost'],
  },
}

module.exports = nextConfig 